<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title><%= trip.date %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>
  <body id="show">
    <div id="show-container">
    <h1 id="here">Here Is Your Trip <a href="/trips/<%= trip._id %>/edit" type="button" class="btn btn-outline-primary">Edit <%= trip.name %></a></h1>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <p id="show-name-date"><%= trip.locationName %> <%= trip.date %></p>
    <p>Latitude: <%= trip.latitude%></p>
    <p>Longitude: <%= trip.longitude%></p>
    <p>Catch Anything? <%= trip.caughtFish %></p>
    <% if (trip.caughtFish) { %>
    <p>Fish: </p>
    <ul class="list-group" id="trip-list-group">
        <% trip.fishInfo.forEach(banana => { %>
          <li class="list-group-item" id="trip-fish-item"><%= banana.species %>, <%= banana.size %>", <%= banana.tackle %></li>
        <% }); %>
    </ul>
    <a href="/trips/<%=trip._id%>/fish/new" type="button" class="btn btn-outline-primary" id="log-a-fish-button">Log a Fish</a>
    <% } %>
    <p>Arrival: <%= trip.timeArrived %></p>
    <p>Departure: <%= trip.timeDeparted %></p>
    <p>Wave Height: <%= trip.weatherInfo[0].waveHeight %> meters</p>
    <p>Wave Direction: <%= trip.weatherInfo[0].waveDirection %> degrees</p>
    <p>Wave Period: <%= trip.weatherInfo[0].wavePeriod %> seconds</p>
    <form action="/trips/<%=trip._id%>?_method=DELETE" method="POST">
        <button type="submit" class="btn btn-danger" id="delete-trip">Delete <%= trip.name %></button>
      </form>
    <br>
    <a class="btn btn-primary" href="/trips/">Back to Trips</a>
  </div>
  <div id="show-img-container">
    <img src="https://imageproxy.ifunny.co/crop:x-20,resize:640x,quality:90x75/images/d73c51aaa66e7af4140114cdfbae4a6461829220f98036ebdfd16ccd344a46c1_1.jpg" alt="man in funny shirt" height="400px" width="350px" >
  </div>
  </body>
</html>